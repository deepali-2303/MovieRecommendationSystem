/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{html,js,svelte,ts}" // Include all Svelte files in the src folder
  ],
  theme: {
    extend: {}, // Customize the theme if needed
  },
  plugins: [],
};
